#!/usr/bin/env bash

export SRC_PATH=${npm_packages_srcPath:-src}
export DIST_PATH=${npm_packages_distPath:-dist}
export TESTS_PATH=${npm_packages_testsPath:-tests}

export NODE_MODULES="$(cd "$(dirname "$(realpath "${BASH_SOURCE[0]}" )" )" && cd ../../.. && pwd)"

