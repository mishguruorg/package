#!/usr/bin/env bash

source $(dirname "$0")/init

echo "> Running eslint"
exec $ESLINT \
  --ext .jsx --ext .js \
  -c $NODE_MODULES/@mishguru/eslint-config/index.js \
  "$@" \
  $SRC_PATH $TESTS_PATH
