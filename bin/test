#!/usr/bin/env bash

export NODE_MODULES="$(cd "$(dirname "$(realpath "${BASH_SOURCE[0]}" )" )" && cd ../../.. && pwd)"
source $NODE_MODULES/@mishguru/package/bin/vars

echo "> Starting AVA"
exec node --harmony-async-await $AVA \
  $(find $SRC_PATH -name $TESTS_NAME) \
  "$@"
